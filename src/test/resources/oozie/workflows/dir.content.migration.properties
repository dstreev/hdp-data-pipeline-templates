# Job Identification
id=MY_JOB_ID

# Job Properties for HOLDINGS operations.build.recon.exports

namenode=${NAMENODE_FS}

# Timestamp
timestamp=${TIMESTAMP}

# FS Details
sourceDir=<source>
destinationDir=<destination>

# Base directory of the Oozie Applications
appsDir=/user/${USER}/${APP_TEMPLATES_BASE}

# Email User
successEmailTo=${EMAIL_TO}
failureEmailTo=${EMAIL_TO}

# Queue
queueName=default

# Assumes the libraries have been configured in Ambari (oozie-site.xml)
oozie.use.system.libpath=true

oozie.wf.application.path=${namenode}${appsDir}/oozie.workflows/dir.content.migration